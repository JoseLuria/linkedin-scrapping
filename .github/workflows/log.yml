name: Console Log

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  log:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm run log
